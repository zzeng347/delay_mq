syntax = "proto3";

package proto;
option go_package ="server/grpc/proto";

service delay {
  rpc RpcServerTest(Req) returns (Replay) {}
}

message Req {
  string id = 1;
}

message Replay {
  int32 code = 1;
  string message = 2;
}
